# Sofia v03 — Ajustes do Cliente

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar todos os ajustes v03 solicitados pelo cliente: labels de autorização, descontos com KM excedido, soft-delete de motoristas, revisões com motorista, gatilho automático de KM, campo locação, delete universal, animações e cascade bidirecional.

**Architecture:** Ajustes em módulos existentes do sistema Sofia (Next.js 16 App Router). Cada task é independente exceto Task 3 (depende de Task 2), Task 6/8 (dependem de Task 5), Task 7 e 8 (Task 8 depende de Task 7 para a action de KM). Ordem recomendada: 1 → 2 → 3 → 4,5,9,11,13 (paralelo) → 6,7 → 8,10,12.

**Tech Stack:** Next.js 16 App Router, TypeScript, Tailwind v4, Supabase (PostgreSQL + RLS), React 19 server/client components, server actions.

## Global Constraints

- Confirmation phrase em todos os DeleteConfirmButton: `'gestão de frotas'` (lowercase, exato)
- Soft-delete em motoristas e equipes: setar `ativo = false`, nunca hard-delete
- Hard-delete em sinistros, revisões, checklist, km_diario (já existe)
- Labels de autorização novos: `'não solicitado'` / `'solicitação feita - X dias sem resposta'` / `'autorizado'`
- Route de API: `GET /api/sofia/veiculo-motorista?veiculo_id=X` ou `?equipe_id=X` ou `?motorista_id=X`
- km_rodados = km_fim − km_inicio (último km_atual do mês − primeiro km_atual do mês, por veículo)
- Pendência de KM: origem `'km_excedido'`, não criar duplicata se já existe pendência aberta para mesma placa/mês
- Animações: `active:scale-95 transition-transform` em botões; `hover:border-[#f05a28] transition-colors` em cards; `transition-colors` em linhas de tabela

---

## Task 1: SQL migration + types

**Files:**
- Create: `sdd-sql-v03.sql`
- Modify: `lib/sofia/types.ts`

**Interfaces:**
- Produces: tipo `PendenciaOrigem` atualizado (inclui `'km_excedido'`); coluna `revisoes.motorista_id`

- [ ] **Step 1: Criar arquivo SQL**

Criar `sdd-sql-v03.sql` na raiz do projeto:

```sql
-- sdd-sql-v03.sql
-- Ajustes v03: motorista_id em revisoes + km_excedido em pendencias

-- 1. revisoes: coluna motorista
ALTER TABLE revisoes ADD COLUMN IF NOT EXISTS motorista_id uuid REFERENCES motoristas(id);

-- 2. pendencias: adicionar km_excedido ao check constraint
ALTER TABLE pendencias DROP CONSTRAINT IF EXISTS pendencias_origem_check;
ALTER TABLE pendencias ADD CONSTRAINT pendencias_origem_check
  CHECK (origem IN ('manual','multa','sinistro','manutencao','documento','termo','km_excedido'));
```

- [ ] **Step 2: Rodar no Supabase Dashboard**

Abrir SQL Editor do Supabase (projeto `iyytcavcgukfjnjjrerx`) e executar o conteúdo de `sdd-sql-v03.sql`.

- [ ] **Step 3: Atualizar tipos TypeScript**

Em `lib/sofia/types.ts`, linha com `PendenciaOrigem` e a interface `Revisao`:

```ts
// Linha ~11 — PendenciaOrigem
export type PendenciaOrigem = 'manual' | 'multa' | 'sinistro' | 'manutencao' | 'documento' | 'termo' | 'km_excedido'

// Interface Revisao — adicionar motorista_id após veiculo_id (~linha 171)
export interface Revisao {
  id: string
  veiculo_id: string
  motorista_id: string | null  // ← adicionar esta linha
  tipo: RevisaoTipo
  fornecedor: string | null
  valor: number | null
  nota_fiscal_storage_path: string | null
  data_realizada: string | null
  km_realizada: number | null
  proxima_data: string | null
  proxima_km: number | null
  status: RevisaoStatus
  observacoes: string | null
  created_at: string
  updated_at: string
}
```

- [ ] **Step 4: Checar tipos**

```bash
npx tsc --noEmit
```

Esperado: sem erros relacionados a PendenciaOrigem ou Revisao.

- [ ] **Step 5: Commit**

```bash
git add sdd-sql-v03.sql lib/sofia/types.ts
git commit -m "feat: SQL v03 (revisoes.motorista_id, pendencias km_excedido) + types"
```

---

## Task 2: Helper compartilhado de autorização

**Files:**
- Create: `lib/sofia/autorizacao.ts`

**Interfaces:**
- Produces:
  - `formatAutorizacaoLabel(status: AutorizacaoStatus, solicitadoEm: string | null): string`
  - `autorizacaoBadgeClass(status: AutorizacaoStatus): string`

- [ ] **Step 1: Criar o helper**

Criar `lib/sofia/autorizacao.ts`:

```ts
import type { AutorizacaoStatus } from './types'

export function formatAutorizacaoLabel(
  status: AutorizacaoStatus,
  solicitadoEm: string | null
): string {
  if (status === 'autorizado') return 'autorizado'
  if (status === 'solicitado') {
    if (!solicitadoEm) return 'solicitação feita - hoje sem resposta'
    const dias = Math.floor((Date.now() - new Date(solicitadoEm).getTime()) / 86400000)
    if (dias === 0) return 'solicitação feita - hoje sem resposta'
    return `solicitação feita - ${dias} dia${dias !== 1 ? 's' : ''} sem resposta`
  }
  return 'não solicitado'
}

export function autorizacaoBadgeClass(status: AutorizacaoStatus): string {
  if (status === 'autorizado') return 'bg-green-900 text-green-300'
  if (status === 'solicitado') return 'bg-amber-900 text-amber-300'
  return 'bg-[#1e3a5f] text-[#94a3b8]'
}
```

- [ ] **Step 2: Checar tipos**

```bash
npx tsc --noEmit
```

Esperado: sem erros.

- [ ] **Step 3: Commit**

```bash
git add lib/sofia/autorizacao.ts
git commit -m "feat: helper compartilhado formatAutorizacaoLabel + autorizacaoBadgeClass"
```

---

## Task 3: Labels renomeados (Multas, Sinistros, KM, Descontos)

**Files:**
- Modify: `app/(operacoes)/sofia/multas/_table.tsx`
- Modify: `app/(operacoes)/sofia/sinistros/page.tsx`
- Modify: `app/(operacoes)/sofia/km/page.tsx`
- Modify: `app/(operacoes)/sofia/descontos/page.tsx`

**Interfaces:**
- Consumes: `formatAutorizacaoLabel`, `autorizacaoBadgeClass` de `lib/sofia/autorizacao.ts` (Task 2)

Em cada arquivo, substituir a lógica inline de label/badge pela chamada ao helper. Remover a função local `diasText` de cada arquivo (foi absorvida pelo helper).

- [ ] **Step 1: Atualizar multas/_table.tsx**

Remover a função `diasText` do topo do arquivo. Adicionar import do helper. Substituir o bloco de label:

```tsx
// Adicionar import no topo (após os imports existentes)
import { formatAutorizacaoLabel, autorizacaoBadgeClass } from '@/lib/sofia/autorizacao'

// Dentro do map de multas, substituir o bloco que cria label/badgeClass:
// ANTES:
// const badgeClass = st === 'autorizado' ? 'bg-green-900 text-green-300' : st === 'solicitado' ? 'bg-amber-900 text-amber-300' : 'bg-[#1e3a5f] text-[#94a3b8]'
// const label = st === 'autorizado' ? 'Autorizado' : st === 'solicitado' ? `Solicitado${dias ? ` · ${dias}` : ''}` : 'Não solicitado'

// DEPOIS:
const st = m.autorizacao_status ?? 'sem_solicitacao'
const badgeClass = autorizacaoBadgeClass(st)
const label = formatAutorizacaoLabel(st, m.autorizacao_solicitado_em ?? null)
```

- [ ] **Step 2: Atualizar sinistros/page.tsx**

Remover `diasText`. Adicionar import. Substituir o bloco inline (linhas ~88-93) por:

```tsx
import { formatAutorizacaoLabel, autorizacaoBadgeClass } from '@/lib/sofia/autorizacao'

// Dentro do map de sinistros:
const st = (sinistroTyped.autorizacao_status ?? 'sem_solicitacao') as AutorizacaoStatus
const badgeClass = autorizacaoBadgeClass(st)
const label = formatAutorizacaoLabel(st, sinistroTyped.autorizacao_solicitado_em ?? null)
```

Adicionar import do tipo: `import type { AutorizacaoStatus } from '@/lib/sofia/types'`

- [ ] **Step 3: Atualizar km/page.tsx**

Remover `diasText`. Adicionar import. Substituir (linhas ~106-131):

```tsx
import { formatAutorizacaoLabel, autorizacaoBadgeClass } from '@/lib/sofia/autorizacao'
import type { AutorizacaoStatus } from '@/lib/sofia/types'

// Dentro do map de resumoMensal:
const authSt = (excEntry?.autorizacao_status ?? (excedido ? 'sem_solicitacao' : null)) as AutorizacaoStatus | null
// No badge:
{authSt != null && (
  <span className={`px-2 py-0.5 rounded text-xs font-medium w-fit ${autorizacaoBadgeClass(authSt)}`}>
    {formatAutorizacaoLabel(authSt, excEntry?.autorizacao_solicitado_em ?? null)}
  </span>
)}
```

- [ ] **Step 4: Atualizar descontos/page.tsx**

Remover `diasText`. Adicionar import. Substituir o badge na coluna Autorização (linha ~130-134):

```tsx
import { formatAutorizacaoLabel, autorizacaoBadgeClass } from '@/lib/sofia/autorizacao'
import type { AutorizacaoStatus } from '@/lib/sofia/types'

// Na célula de Autorização:
const st = l.autorizacao_status as AutorizacaoStatus
return (
  <span className={`px-2 py-0.5 rounded text-xs font-medium ${autorizacaoBadgeClass(st)}`}>
    {formatAutorizacaoLabel(st, l.autorizacao_solicitado_em)}
  </span>
)
```

- [ ] **Step 5: Checar tipos e build**

```bash
npx tsc --noEmit
```

Esperado: sem erros.

- [ ] **Step 6: Commit**

```bash
git add app/(operacoes)/sofia/multas/_table.tsx app/(operacoes)/sofia/sinistros/page.tsx app/(operacoes)/sofia/km/page.tsx app/(operacoes)/sofia/descontos/page.tsx
git commit -m "feat: labels de autorização padronizados (solicitação feita - X dias sem resposta)"
```

---

## Task 4: Motoristas — soft-delete

**Files:**
- Modify: `app/(operacoes)/sofia/motoristas/_actions.ts`
- Modify: `app/(operacoes)/sofia/motoristas/page.tsx`
- Modify: `app/(operacoes)/sofia/motoristas/[id]/page.tsx`

**Interfaces:**
- Produces: `desativarMotoristaAction(prev, formData): Promise<{error?,success?}>`

- [ ] **Step 1: Adicionar action em _actions.ts**

```ts
// Adicionar ao final de app/(operacoes)/sofia/motoristas/_actions.ts

export async function desativarMotoristaAction(
  _prev: { error?: string; success?: boolean },
  formData: FormData
): Promise<{ error?: string; success?: boolean }> {
  const id = formData.get('id') as string
  if (!id) return { error: 'ID inválido' }

  const supabase = await createClient()
  const { error } = await supabase
    .from('motoristas')
    .update({ ativo: false })
    .eq('id', id)

  if (error) return { error: 'Erro ao desativar motorista' }

  revalidatePath('/sofia/motoristas')
  return { success: true }
}
```

- [ ] **Step 2: Adicionar DeleteConfirmButton na lista de motoristas**

Em `app/(operacoes)/sofia/motoristas/page.tsx`, ler o arquivo atual e adicionar:

```tsx
// Adicionar imports
import DeleteConfirmButton from '@/components/sofia/DeleteConfirmButton'
import { desativarMotoristaAction } from './_actions'

// Na linha de cada motorista, na última coluna, adicionar:
<td className="px-4 py-3 text-right">
  <DeleteConfirmButton action={desativarMotoristaAction} id={m.id} label="Desativar" />
</td>

// No <thead>, adicionar coluna vazia para alinhar:
<th className="px-4 py-3"></th>
```

Motoristas inativos: adicionar badge e ordenar por `ativo DESC`:

```tsx
// Na query de getMotoristas() em lib/sofia/queries.ts, verificar se retorna campo ativo
// Se sim, filtrar na exibição: motoristas ativos primeiro, inativos ao final com badge cinza
// Badge inativo:
{!m.ativo && (
  <span className="ml-2 px-1.5 py-0.5 rounded text-xs bg-[#1e3a5f] text-[#4a6080]">Inativo</span>
)}
```

- [ ] **Step 3: Adicionar DeleteConfirmButton no detalhe do motorista**

Em `app/(operacoes)/sofia/motoristas/[id]/page.tsx`, adicionar zona de perigo no final da página:

```tsx
import DeleteConfirmButton from '@/components/sofia/DeleteConfirmButton'
import { desativarMotoristaAction } from '../_actions'

// Adicionar ao final do JSX (antes do fechamento do div principal):
<div className="mt-10 p-4 rounded-xl border border-red-800 bg-red-950/20">
  <h2 className="text-sm font-medium text-red-400 uppercase tracking-wider mb-3">Zona de Perigo</h2>
  <p className="text-[#94a3b8] text-sm mb-3">
    Desativar este motorista remove-o das listagens ativas. O histórico de multas, sinistros e checklists é preservado.
  </p>
  <DeleteConfirmButton action={desativarMotoristaAction} id={motorista.id} label="Desativar Motorista" />
</div>
```

- [ ] **Step 4: Checar tipos**

```bash
npx tsc --noEmit
```

- [ ] **Step 5: Commit**

```bash
git add app/(operacoes)/sofia/motoristas/_actions.ts app/(operacoes)/sofia/motoristas/page.tsx "app/(operacoes)/sofia/motoristas/[id]/page.tsx"
git commit -m "feat: motoristas soft-delete com confirmação por texto"
```

---

## Task 5: Route handler /api/sofia/veiculo-motorista

**Files:**
- Create: `app/api/sofia/veiculo-motorista/route.ts`

**Interfaces:**
- Produces:
  - `GET ?veiculo_id=X` → `{ motorista_id, equipe_id, motoristas: {id,nome}|null, equipes: {id,codigo}|null } | null`
  - `GET ?equipe_id=X` → `{ veiculo: {id,placa,modelo}|null, motorista: {id,nome}|null }`
  - `GET ?motorista_id=X` → `{ equipe: {id,codigo}|null, veiculo: {id,placa,modelo}|null }`

- [ ] **Step 1: Criar route handler**

```ts
// app/api/sofia/veiculo-motorista/route.ts
import { createClient } from '@/lib/supabase/server'
import { NextResponse } from 'next/server'

export async function GET(request: Request) {
  const { searchParams } = new URL(request.url)
  const veiculo_id = searchParams.get('veiculo_id')
  const equipe_id = searchParams.get('equipe_id')
  const motorista_id = searchParams.get('motorista_id')

  const supabase = await createClient()

  if (veiculo_id) {
    const { data } = await supabase
      .from('veiculo_responsabilidade_historico')
      .select('motorista_id, equipe_id, motoristas(id, nome), equipes(id, codigo)')
      .eq('veiculo_id', veiculo_id)
      .is('fim', null)
      .maybeSingle()
    return NextResponse.json(data ?? null)
  }

  if (equipe_id) {
    const [{ data: veiculo }, { data: motorista }] = await Promise.all([
      supabase
        .from('veiculos')
        .select('id, placa, modelo')
        .eq('equipe_id', equipe_id)
        .eq('status', 'ativo')
        .maybeSingle(),
      supabase
        .from('motoristas')
        .select('id, nome')
        .eq('equipe_id', equipe_id)
        .eq('ativo', true)
        .maybeSingle(),
    ])
    return NextResponse.json({ veiculo: veiculo ?? null, motorista: motorista ?? null })
  }

  if (motorista_id) {
    const { data: mot } = await supabase
      .from('motoristas')
      .select('equipe_id, equipes(id, codigo)')
      .eq('id', motorista_id)
      .maybeSingle()

    if (!mot?.equipe_id) return NextResponse.json({ equipe: null, veiculo: null })

    const { data: veiculo } = await supabase
      .from('veiculos')
      .select('id, placa, modelo')
      .eq('equipe_id', mot.equipe_id)
      .eq('status', 'ativo')
      .maybeSingle()

    return NextResponse.json({
      equipe: (mot as { equipe_id: string; equipes: { id: string; codigo: string } | null }).equipes ?? null,
      veiculo: veiculo ?? null,
    })
  }

  return NextResponse.json(null, { status: 400 })
}
```

- [ ] **Step 2: Checar tipos**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add app/api/sofia/veiculo-motorista/route.ts
git commit -m "feat: route handler /api/sofia/veiculo-motorista (cascade bidirecional)"
```

---

## Task 6: Revisões — coluna motorista + auto-fill no form

**Files:**
- Modify: `lib/sofia/queries.ts` (getRevisoes)
- Modify: `app/(operacoes)/sofia/revisoes/page.tsx`
- Modify: `app/(operacoes)/sofia/revisoes/nova/_form.tsx`
- Modify: `app/(operacoes)/sofia/revisoes/_actions.ts`
- Modify: `app/(operacoes)/sofia/revisoes/nova/page.tsx`

**Interfaces:**
- Consumes: `GET /api/sofia/veiculo-motorista?veiculo_id=X` (Task 5)

- [ ] **Step 1: Atualizar getRevisoes para incluir motorista**

Em `lib/sofia/queries.ts`, na função `getRevisoes`:

```ts
export async function getRevisoes() {
  const supabase = await createClient()
  const { data } = await supabase
    .from('revisoes')
    .select('*, veiculos(placa), motoristas(nome)')
    .order('created_at', { ascending: false })
  return data ?? []
}
```

- [ ] **Step 2: Adicionar coluna Motorista na lista**

Em `app/(operacoes)/sofia/revisoes/page.tsx`:

```tsx
// Atualizar tipo local
type RevisaoComVeiculo = Revisao & {
  veiculos: { placa: string } | null
  motoristas: { nome: string } | null
}

// No <thead>, adicionar após "Veículo":
<th className="text-left px-4 py-3 text-[#4a6080] font-medium">Motorista</th>

// No <tbody>, em cada linha, adicionar após a célula de placa:
<td className="px-4 py-3 text-[#94a3b8]">{r.motoristas?.nome ?? '—'}</td>
```

- [ ] **Step 3: Passar motoristas para o form**

Em `app/(operacoes)/sofia/revisoes/nova/page.tsx`:

```tsx
import { getVeiculos, getMotoristas } from '@/lib/sofia/queries'
import NovaRevisaoForm from './_form'

export default async function NovaRevisaoPage() {
  const [veiculos, motoristas] = await Promise.all([getVeiculos(), getMotoristas()])
  return <NovaRevisaoForm veiculos={veiculos} motoristas={motoristas} />
}
```

- [ ] **Step 4: Adicionar auto-fill no form de nova revisão**

Em `app/(operacoes)/sofia/revisoes/nova/_form.tsx`, converter para `'use client'` se não for, adicionar cascade:

```tsx
'use client'
import { useActionState, useEffect, useState } from 'react'
import { useRouter } from 'next/navigation'
import { criarRevisaoAction } from '../_actions'
import type { Veiculo, Motorista } from '@/lib/sofia/types'

export default function NovaRevisaoForm({
  veiculos,
  motoristas,
}: {
  veiculos: Veiculo[]
  motoristas: Motorista[]
}) {
  const [state, action, isPending] = useActionState(criarRevisaoAction, {})
  const router = useRouter()
  const [motoristaId, setMotoristaId] = useState('')

  useEffect(() => {
    if (state.success) router.push('/sofia/revisoes')
  }, [state.success, router])

  async function handleVeiculoChange(veiculoId: string) {
    if (!veiculoId) { setMotoristaId(''); return }
    const res = await fetch(`/api/sofia/veiculo-motorista?veiculo_id=${veiculoId}`)
    const data = await res.json()
    if (data?.motoristas?.id) setMotoristaId(data.motoristas.id)
    else setMotoristaId('')
  }

  return (
    <div className="p-8 max-w-md">
      <h1 className="text-2xl font-bold text-white mb-2">Registrar Revisão</h1>
      <p className="text-[#4a6080] text-sm mb-8">Manutenção preventiva ou corretiva</p>

      <form action={action} className="flex flex-col gap-4">
        {state.error && (
          <div className="px-4 py-3 rounded-lg border border-red-600 bg-red-950 text-red-300 text-sm">
            {state.error}
          </div>
        )}

        <div className="flex flex-col gap-1.5">
          <label className="text-sm text-[#94a3b8]">Veículo *</label>
          <select
            name="veiculo_id"
            required
            onChange={(e) => handleVeiculoChange(e.target.value)}
            className="px-3 py-2.5 rounded-lg bg-[#0f1f3d] border border-[#1e3a5f] text-white focus:outline-none focus:border-[#f05a28] text-sm"
          >
            <option value="">Selecione</option>
            {veiculos.map((v) => (
              <option key={v.id} value={v.id}>{v.placa} · {v.modelo}</option>
            ))}
          </select>
        </div>

        <div className="flex flex-col gap-1.5">
          <label className="text-sm text-[#94a3b8]">Motorista responsável</label>
          <select
            name="motorista_id"
            value={motoristaId}
            onChange={(e) => setMotoristaId(e.target.value)}
            className="px-3 py-2.5 rounded-lg bg-[#0f1f3d] border border-[#1e3a5f] text-white focus:outline-none focus:border-[#f05a28] text-sm"
          >
            <option value="">Selecione ou auto-preenchido</option>
            {motoristas.map((m) => (
              <option key={m.id} value={m.id}>{m.nome}</option>
            ))}
          </select>
        </div>

        {/* manter os demais campos existentes: tipo, fornecedor, valor, datas, km, observações */}
        <div className="flex flex-col gap-1.5">
          <label className="text-sm text-[#94a3b8]">Tipo *</label>
          <select name="tipo" required defaultValue="preventiva" className="px-3 py-2.5 rounded-lg bg-[#0f1f3d] border border-[#1e3a5f] text-white focus:outline-none focus:border-[#f05a28] text-sm">
            <option value="preventiva">Preventiva</option>
            <option value="corretiva">Corretiva</option>
          </select>
        </div>

        <div className="flex flex-col gap-1.5">
          <label className="text-sm text-[#94a3b8]">Fornecedor</label>
          <input name="fornecedor" placeholder="Nome da oficina/fornecedor" className="px-3 py-2.5 rounded-lg bg-[#0f1f3d] border border-[#1e3a5f] text-white placeholder-[#4a6080] focus:outline-none focus:border-[#f05a28] text-sm" />
        </div>

        <div className="flex flex-col gap-1.5">
          <label className="text-sm text-[#94a3b8]">Valor (R$)</label>
          <input name="valor" type="number" step="0.01" placeholder="0.00" className="px-3 py-2.5 rounded-lg bg-[#0f1f3d] border border-[#1e3a5f] text-white placeholder-[#4a6080] focus:outline-none focus:border-[#f05a28] text-sm" />
        </div>

        <div className="grid grid-cols-2 gap-3">
          <div className="flex flex-col gap-1.5">
            <label className="text-sm text-[#94a3b8]">Data realizada</label>
            <input name="data_realizada" type="date" className="px-3 py-2.5 rounded-lg bg-[#0f1f3d] border border-[#1e3a5f] text-white focus:outline-none focus:border-[#f05a28] text-sm [color-scheme:dark]" />
          </div>
          <div className="flex flex-col gap-1.5">
            <label className="text-sm text-[#94a3b8]">KM realizada</label>
            <input name="km_realizada" type="number" placeholder="0" className="px-3 py-2.5 rounded-lg bg-[#0f1f3d] border border-[#1e3a5f] text-white placeholder-[#4a6080] focus:outline-none focus:border-[#f05a28] text-sm" />
          </div>
        </div>

        <div className="grid grid-cols-2 gap-3">
          <div className="flex flex-col gap-1.5">
            <label className="text-sm text-[#94a3b8]">Próxima data prevista</label>
            <input name="proxima_data" type="date" className="px-3 py-2.5 rounded-lg bg-[#0f1f3d] border border-[#1e3a5f] text-white focus:outline-none focus:border-[#f05a28] text-sm [color-scheme:dark]" />
          </div>
          <div className="flex flex-col gap-1.5">
            <label className="text-sm text-[#94a3b8]">Próxima KM prevista</label>
            <input name="proxima_km" type="number" placeholder="0" className="px-3 py-2.5 rounded-lg bg-[#0f1f3d] border border-[#1e3a5f] text-white placeholder-[#4a6080] focus:outline-none focus:border-[#f05a28] text-sm" />
          </div>
        </div>

        <div className="flex flex-col gap-1.5">
          <label className="text-sm text-[#94a3b8]">Observações</label>
          <textarea name="observacoes" rows={2} className="px-3 py-2.5 rounded-lg bg-[#0f1f3d] border border-[#1e3a5f] text-white placeholder-[#4a6080] focus:outline-none focus:border-[#f05a28] text-sm resize-none" />
        </div>

        <div className="flex gap-3 mt-2">
          <button type="button" onClick={() => router.back()} className="flex-1 py-2.5 rounded-lg border border-[#1e3a5f] text-[#94a3b8] text-sm hover:border-[#94a3b8] transition-colors">
            Cancelar
          </button>
          <button type="submit" disabled={isPending} className="flex-1 py-2.5 rounded-lg bg-[#f05a28] text-white text-sm font-medium hover:bg-[#d94e22] disabled:opacity-50 transition-colors">
            {isPending ? 'Salvando...' : 'Registrar Revisão'}
          </button>
        </div>
      </form>
    </div>
  )
}
```

- [ ] **Step 5: Atualizar criarRevisaoAction para salvar motorista_id**

Em `app/(operacoes)/sofia/revisoes/_actions.ts`:

```ts
export async function criarRevisaoAction(_prev: State, formData: FormData): Promise<State> {
  const veiculo_id = formData.get('veiculo_id') as string
  const motorista_id = (formData.get('motorista_id') as string) || null  // ← adicionar
  const tipo = formData.get('tipo') as string
  const fornecedor = (formData.get('fornecedor') as string).trim() || null
  const valor = formData.get('valor') ? Number(formData.get('valor')) : null
  const data_realizada = (formData.get('data_realizada') as string) || null
  const km_realizada = formData.get('km_realizada') ? Number(formData.get('km_realizada')) : null
  const proxima_data = (formData.get('proxima_data') as string) || null
  const proxima_km = formData.get('proxima_km') ? Number(formData.get('proxima_km')) : null
  const observacoes = (formData.get('observacoes') as string).trim() || null

  if (!veiculo_id || !tipo) return { error: 'Veículo e tipo são obrigatórios' }

  const status = proxima_data && new Date(proxima_data) < new Date() ? 'atrasada' : proxima_data ? 'agendada' : 'em_dia'

  const supabase = await createClient()
  const { error } = await supabase.from('revisoes').insert({
    veiculo_id, motorista_id, tipo, fornecedor, valor, data_realizada,
    km_realizada, proxima_data, proxima_km, status, observacoes,
  })

  if (error) return { error: 'Erro ao registrar revisão' }
  revalidatePath('/sofia/revisoes')
  return { success: true }
}
```

- [ ] **Step 6: Checar tipos**

```bash
npx tsc --noEmit
```

- [ ] **Step 7: Commit**

```bash
git add lib/sofia/queries.ts "app/(operacoes)/sofia/revisoes/page.tsx" "app/(operacoes)/sofia/revisoes/nova/_form.tsx" "app/(operacoes)/sofia/revisoes/_actions.ts" "app/(operacoes)/sofia/revisoes/nova/page.tsx"
git commit -m "feat: revisões — coluna motorista + auto-fill ao selecionar veículo"
```

---

## Task 7: KM — gatilho automático de excedência + action de autorização

**Files:**
- Modify: `app/(operacoes)/sofia/km/_actions.ts`

**Interfaces:**
- Produces: `atualizarAutorizacaoKmExcedidoAction(formData): Promise<void>` (para usar em Descontos, Task 8)

- [ ] **Step 1: Adicionar gatilho automático em lancarKmAction**

Em `app/(operacoes)/sofia/km/_actions.ts`, adicionar a função helper `verificarERegistrarExcedencia` e chamá-la ao final de `lancarKmAction`:

```ts
// Adicionar após os imports existentes — função interna, não exportada
async function verificarERegistrarExcedencia(
  supabase: Awaited<ReturnType<typeof import('@/lib/supabase/server').createClient>>,
  veiculoId: string,
  data: string
) {
  const mes = data.slice(0, 7) // '2026-06'
  const mesInicio = `${mes}-01`
  const [ano, mesNum] = mes.split('-').map(Number)
  const proximoMes = mesNum === 12
    ? `${ano + 1}-01-01`
    : `${ano}-${String(mesNum + 1).padStart(2, '0')}-01`

  const [{ data: veiculo }, { data: lancamentos }] = await Promise.all([
    supabase.from('veiculos').select('km_contratual_mensal, placa').eq('id', veiculoId).single(),
    supabase.from('km_diario').select('km_atual').eq('veiculo_id', veiculoId)
      .gte('data', mesInicio).lt('data', proximoMes).order('data', { ascending: true }),
  ])

  if (!veiculo?.km_contratual_mensal || !lancamentos || lancamentos.length < 2) return

  const kmRodados = lancamentos[lancamentos.length - 1].km_atual - lancamentos[0].km_atual
  if (kmRodados <= veiculo.km_contratual_mensal) return

  // Upsert km_excedido_desconto
  const { data: existing } = await supabase
    .from('km_excedido_desconto')
    .select('id')
    .eq('veiculo_id', veiculoId)
    .eq('mes', mesInicio)
    .maybeSingle()

  if (existing) {
    await supabase
      .from('km_excedido_desconto')
      .update({ km_realizado: lancamentos[lancamentos.length - 1].km_atual, km_contratual: veiculo.km_contratual_mensal })
      .eq('id', existing.id)
  } else {
    await supabase.from('km_excedido_desconto').insert({
      veiculo_id: veiculoId,
      mes: mesInicio,
      km_contratual: veiculo.km_contratual_mensal,
      km_realizado: lancamentos[lancamentos.length - 1].km_atual,
      autorizacao_status: 'sem_solicitacao',
    })

    // Criar pendência (evitar duplicata)
    const dataFormatada = new Date(mesInicio).toLocaleDateString('pt-BR', { month: 'long', year: 'numeric' })
    const descricao = `KM excedido — ${veiculo.placa} ${dataFormatada}`
    const { data: pendenciaExistente } = await supabase
      .from('pendencias')
      .select('id')
      .eq('descricao', descricao)
      .eq('status', 'aberta')
      .maybeSingle()

    if (!pendenciaExistente) {
      await supabase.from('pendencias').insert({
        descricao,
        origem: 'km_excedido',
        status: 'aberta',
      })
    }
  }
}
```

Em `lancarKmAction`, após o `revalidatePath('/sofia/km')` e antes do `return { success: true }`:

```ts
  // Verificar excedência e criar pendência/desconto automaticamente
  await verificarERegistrarExcedencia(supabase, veiculo_id, data)
  revalidatePath('/sofia/descontos')
  revalidatePath('/sofia/pendencias')
```

- [ ] **Step 2: Adicionar atualizarAutorizacaoKmExcedidoAction**

Mesma assinatura `(id, formData)` das ações de multa/sinistro para poder usar `.bind(null, id)` em Descontos:

```ts
export async function atualizarAutorizacaoKmExcedidoAction(id: string, formData: FormData): Promise<void> {
  const status = formData.get('status') as string
  if (!id || !['sem_solicitacao', 'solicitado', 'autorizado'].includes(status)) return

  const supabase = await createClient()
  const update: Record<string, unknown> = { autorizacao_status: status }
  if (status === 'solicitado') update.autorizacao_solicitado_em = new Date().toISOString()
  if (status === 'sem_solicitacao') update.autorizacao_solicitado_em = null

  await supabase.from('km_excedido_desconto').update(update).eq('id', id)
  revalidatePath('/sofia/km')
  revalidatePath('/sofia/descontos')
  revalidatePath('/sofia/motoristas')
}
```

- [ ] **Step 3: Checar tipos**

```bash
npx tsc --noEmit
```

- [ ] **Step 4: Commit**

```bash
git add "app/(operacoes)/sofia/km/_actions.ts"
git commit -m "feat: KM auto-cria pendência e km_excedido_desconto ao ultrapassar contratual"
```

---

## Task 8: Descontos — botões inline + linhas KM excedido

**Files:**
- Modify: `app/(operacoes)/sofia/descontos/page.tsx`

**Interfaces:**
- Consumes: `atualizarAutorizacaoKmExcedidoAction` (Task 7); `atualizarAutorizacaoMultaAction`, `atualizarAutorizacaoSinistroAction` (já existentes)

- [ ] **Step 1: Adicionar linhas KM excedido e botões inline**

Substituir `app/(operacoes)/sofia/descontos/page.tsx` integralmente:

```tsx
import { createClient } from '@/lib/supabase/server'
import type { Multa, Sinistro, KmExcedidoDesconto } from '@/lib/sofia/types'
import type { AutorizacaoStatus } from '@/lib/sofia/types'
import { formatAutorizacaoLabel, autorizacaoBadgeClass } from '@/lib/sofia/autorizacao'
import {
  atualizarStatusMultaAction,
  registrarDescontoMultaAction,
  desfazerDescontoMultaAction,
  atualizarStatusDescontoSinistroAction,
  registrarDescontoSinistroAction,
  desfazerDescontoSinistroAction,
  atualizarAutorizacaoMultaAction,
} from './multas-actions'
import { atualizarAutorizacaoSinistroAction } from './sinistros-actions'
import { atualizarAutorizacaoKmExcedidoAction } from '../km/_actions'
```

> **Nota:** as imports de `atualizarAutorizacaoMultaAction` e `atualizarAutorizacaoSinistroAction` já existem em seus módulos originais. Em descontos/page.tsx importar diretamente dos paths originais:
> - `from '../multas/_actions'` para `atualizarAutorizacaoMultaAction`
> - `from '../sinistros/_actions'` para `atualizarAutorizacaoSinistroAction`
> - `from '../km/_actions'` para `atualizarAutorizacaoKmExcedidoAction`

Atualizar o tipo `LinhaDesconto`:

```ts
type LinhaDesconto = {
  origem: 'multa' | 'sinistro' | 'km_excedido'
  id: string
  data: string
  veiculoPlaca: string | null
  motoristaNome: string | null
  valor: number
  status: string
  valor_descontado: number | null
  tipo_desconto: string
  autorizacao_assinada: boolean
  autorizacao_status: AutorizacaoStatus
  autorizacao_solicitado_em: string | null
  // Para km_excedido:
  km_contratual?: number
  km_realizado?: number
}
```

No data fetch, adicionar busca de km_excedido_desconto:

```ts
const [{ data: multas }, { data: sinistros }, { data: excedidos }] = await Promise.all([
  supabase.from('multas').select('*, veiculos(placa), motoristas(nome)').order('data', { ascending: false }),
  supabase.from('sinistros').select('*, veiculos(placa), motoristas(nome)').order('data', { ascending: false }),
  supabase.from('km_excedido_desconto').select('*, veiculos(placa), motoristas(nome)').order('mes', { ascending: false }),
])

const linhasKm: LinhaDesconto[] = ((excedidos ?? []) as (KmExcedidoDesconto & { veiculos: { placa: string } | null; motoristas: { nome: string } | null })[]).map((e) => ({
  origem: 'km_excedido',
  id: e.id,
  data: e.mes,
  veiculoPlaca: e.veiculos?.placa ?? null,
  motoristaNome: e.motoristas?.nome ?? null,
  valor: 0,
  status: 'km_excedido',
  valor_descontado: null,
  tipo_desconto: 'nenhum',
  autorizacao_assinada: false,
  autorizacao_status: e.autorizacao_status,
  autorizacao_solicitado_em: e.autorizacao_solicitado_em,
  km_contratual: e.km_contratual,
  km_realizado: e.km_realizado,
}))

const linhas = [...linhasMultas, ...linhasSinistros, ...linhasKm].sort(
  (a, b) => new Date(b.data).getTime() - new Date(a.data).getTime()
)
```

Na tabela, adicionar coluna de botões de autorização inline (após o badge de status):

```tsx
<td className="px-4 py-3">
  {(() => {
    const st = l.autorizacao_status
    const autorizacaoAction =
      l.origem === 'multa'
        ? atualizarAutorizacaoMultaAction
        : l.origem === 'sinistro'
        ? atualizarAutorizacaoSinistroAction
        : atualizarAutorizacaoKmExcedidoAction
    return (
      <div className="flex flex-col gap-1">
        <span className={`px-2 py-0.5 rounded text-xs font-medium w-fit ${autorizacaoBadgeClass(st)}`}>
          {formatAutorizacaoLabel(st, l.autorizacao_solicitado_em)}
        </span>
        <div className="flex gap-2">
          {st === 'sem_solicitacao' && (
            <form action={autorizacaoAction.bind(null, l.id)}>
              <button name="status" value="solicitado" type="submit" className="text-xs text-amber-400 hover:underline">Solicitar</button>
            </form>
          )}
          {st === 'solicitado' && (
            <>
              <form action={autorizacaoAction.bind(null, l.id)}>
                <button name="status" value="autorizado" type="submit" className="text-xs text-green-400 hover:underline">Autorizar</button>
              </form>
              <form action={autorizacaoAction.bind(null, l.id)}>
                <button name="status" value="sem_solicitacao" type="submit" className="text-xs text-[#4a6080] hover:underline">← Cancelar</button>
              </form>
            </>
          )}
          {st === 'autorizado' && (
            <form action={autorizacaoAction.bind(null, l.id)}>
              <button name="status" value="solicitado" type="submit" className="text-xs text-[#4a6080] hover:underline">← Revogar</button>
            </form>
          )}
        </div>
      </div>
    )
  })()}
</td>
```

> **Nota:** `atualizarAutorizacaoKmExcedidoAction` (Task 7) usa assinatura `(id: string, formData: FormData)` — igual a multas/sinistros — então `.bind(null, l.id)` funciona diretamente. `upsertKmExcedidoStatusAction` continua existindo para o fluxo da página KM (usa hidden inputs para enviar veiculo_id, mes etc.).

- [ ] **Step 2: Valor para km_excedido**

Na coluna Valor, para km_excedido mostrar o excedente em km:

```tsx
<td className="px-4 py-3 text-white text-right font-medium">
  {l.origem === 'km_excedido'
    ? `+${((l.km_realizado ?? 0) - (l.km_contratual ?? 0)).toLocaleString('pt-BR')} km`
    : `R$ ${Number(l.valor).toFixed(2)}`}
</td>
```

- [ ] **Step 3: Checar tipos**

```bash
npx tsc --noEmit
```

- [ ] **Step 4: Commit**

```bash
git add "app/(operacoes)/sofia/descontos/page.tsx" "app/(operacoes)/sofia/km/_actions.ts"
git commit -m "feat: descontos — botões autorização inline + linhas KM excedido"
```

---

## Task 9: Veículos — campo valor_locacao_mensal

**Files:**
- Modify: `app/(operacoes)/sofia/veiculos/novo/_form.tsx`
- Modify: `app/(operacoes)/sofia/veiculos/[id]/page.tsx`
- Modify: `app/(operacoes)/sofia/veiculos/_actions.ts`

- [ ] **Step 1: Adicionar campo no form de novo veículo**

Em `app/(operacoes)/sofia/veiculos/novo/_form.tsx`, no array de campos, adicionar após `km_contratual_mensal`:

```tsx
{ name: 'valor_locacao_mensal', label: 'Valor locação/mês (R$)', placeholder: '2500.00', type: 'number', required: false },
```

Atualizar a `criarVeiculoAction` em `_actions.ts` para salvar o campo:

```ts
// Em app/(operacoes)/sofia/veiculos/_actions.ts — criarVeiculoAction
const valor_locacao_mensal = formData.get('valor_locacao_mensal')
  ? Number(formData.get('valor_locacao_mensal'))
  : null

// Adicionar no insert:
.insert({ placa, modelo, ano, km_atual, km_contratual_mensal, equipe_id, valor_locacao_mensal })
```

- [ ] **Step 2: Adicionar edição inline no detalhe do veículo**

Em `app/(operacoes)/sofia/veiculos/[id]/page.tsx`, adicionar uma seção "Locação":

Adicionar action `atualizarLocacaoVeiculoAction` em `_actions.ts`:

```ts
export async function atualizarLocacaoVeiculoAction(
  _prev: { error?: string; success?: boolean },
  formData: FormData
): Promise<{ error?: string; success?: boolean }> {
  const id = formData.get('id') as string
  const valor_locacao_mensal = formData.get('valor_locacao_mensal')
    ? Number(formData.get('valor_locacao_mensal'))
    : null

  const supabase = await createClient()
  const { error } = await supabase
    .from('veiculos')
    .update({ valor_locacao_mensal })
    .eq('id', id)

  if (error) return { error: 'Erro ao atualizar locação' }
  revalidatePath(`/sofia/veiculos/${id}`)
  revalidatePath('/sofia/custos')
  return { success: true }
}
```

Criar componente inline no detalhe (client component separado `_locacao-form.tsx` se necessário, ou usar form server action inline):

```tsx
// Em veiculos/[id]/page.tsx, adicionar seção:
<div className="mt-6">
  <h2 className="text-sm font-medium text-[#4a6080] uppercase tracking-wider mb-3">Locação mensal</h2>
  <div className="p-4 rounded-xl border border-[#1e3a5f] bg-[#0d2050]">
    <form action={atualizarLocacaoVeiculoAction} className="flex gap-2 items-end">
      <input type="hidden" name="id" value={veiculo.id} />
      <div className="flex flex-col gap-1">
        <label className="text-xs text-[#94a3b8]">Valor locação/mês (R$)</label>
        <input
          name="valor_locacao_mensal"
          type="number"
          step="0.01"
          defaultValue={veiculo.valor_locacao_mensal ?? ''}
          placeholder="0.00"
          className="px-3 py-2 rounded-lg bg-[#0f1f3d] border border-[#1e3a5f] text-white text-sm focus:outline-none focus:border-[#f05a28] w-36"
        />
      </div>
      <button
        type="submit"
        className="px-3 py-2 rounded-lg bg-[#f05a28] text-white text-sm font-medium hover:bg-[#d94e22] transition-colors active:scale-95"
      >
        Salvar
      </button>
    </form>
  </div>
</div>
```

- [ ] **Step 3: Checar tipos**

```bash
npx tsc --noEmit
```

- [ ] **Step 4: Commit**

```bash
git add "app/(operacoes)/sofia/veiculos/novo/_form.tsx" "app/(operacoes)/sofia/veiculos/[id]/page.tsx" "app/(operacoes)/sofia/veiculos/_actions.ts"
git commit -m "feat: veículos — campo valor_locacao_mensal no form de cadastro e edição inline"
```

---

## Task 10: Custos — placa clicável + coluna KM/mês

**Files:**
- Modify: `app/(operacoes)/sofia/custos/page.tsx`

- [ ] **Step 1: Placa clicável + coluna KM**

Em `app/(operacoes)/sofia/custos/page.tsx`:

```tsx
import Link from 'next/link'
import { getVeiculos, getCentroCustoHistorico, getKmResumoMensal } from '@/lib/sofia/queries'

// Adicionar fetch de kmResumoMensal e filtrar para o mês atual
const mesAtual = hoje.toISOString().slice(0, 7) // '2026-06'
const kmResumo = await getKmResumoMensal()
const kmMesAtual = new Map(
  kmResumo.filter((r) => r.mes === mesAtual).map((r) => [r.veiculo_id, r])
)
```

Na tabela, adicionar coluna "KM mês" no thead:

```tsx
<th className="text-right px-4 py-3 text-[#4a6080] font-medium">KM mês</th>
```

Na linha de cada veículo, tornar a placa um link e adicionar célula de KM:

```tsx
// Placa clicável:
<td className="px-4 py-3 font-medium font-mono">
  <Link
    href={`/sofia/veiculos/${l.veiculo.id}`}
    className="text-white hover:text-[#f05a28] transition-colors hover:underline"
  >
    {l.veiculo.placa}
  </Link>
</td>

// Coluna KM mês:
{(() => {
  const km = kmMesAtual.get(l.veiculo.id)
  if (!km) return <td className="px-4 py-3 text-[#4a6080] text-right text-sm">—</td>
  const excedido = km.km_rodados > (km.km_contratual_mensal ?? Infinity)
  return (
    <td className={`px-4 py-3 text-right text-sm font-mono ${excedido ? 'text-red-400' : 'text-[#94a3b8]'}`}>
      {km.km_contratual_mensal != null
        ? `${km.km_contratual_mensal.toLocaleString('pt-BR')} / ${km.km_rodados.toLocaleString('pt-BR')} km${excedido ? ' ⚠' : ''}`
        : `${km.km_rodados.toLocaleString('pt-BR')} km`}
    </td>
  )
})()}
```

- [ ] **Step 2: Checar tipos**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add "app/(operacoes)/sofia/custos/page.tsx"
git commit -m "feat: custos — placa clicável (link pro veículo) + coluna KM contratado/rodado"
```

---

## Task 11: Delete universal

**Files:**
- Modify: `app/(operacoes)/sofia/multas/_table.tsx` (substituir window.confirm)
- Modify: `app/(operacoes)/sofia/multas/_actions.ts` (assinatura compat com DeleteConfirmButton)
- Modify: `app/(operacoes)/sofia/sinistros/page.tsx` (adicionar delete)
- Modify: `app/(operacoes)/sofia/sinistros/_actions.ts` (adicionar excluirSinistroAction)
- Modify: `app/(operacoes)/sofia/equipes/page.tsx` (adicionar soft-delete)
- Modify: `app/(operacoes)/sofia/equipes/_actions.ts` (adicionar desativarEquipeAction)
- Modify: `app/(operacoes)/sofia/revisoes/page.tsx` (adicionar delete)
- Modify: `app/(operacoes)/sofia/revisoes/_actions.ts` (adicionar excluirRevisaoAction)
- Modify: `app/(operacoes)/sofia/checklist/page.tsx` (adicionar delete)
- Modify: `app/(operacoes)/sofia/checklist/_actions.ts` (adicionar excluirChecklistAction)

- [ ] **Step 1: Multas — substituir window.confirm por DeleteConfirmButton**

Em `app/(operacoes)/sofia/multas/_actions.ts`, verificar se `excluirMultaAction` já tem assinatura `(prev, formData)`. Se tiver `(id: string)`, ajustar para:

```ts
export async function excluirMultaAction(
  _prev: { error?: string; success?: boolean },
  formData: FormData
): Promise<{ error?: string; success?: boolean }> {
  const id = formData.get('id') as string
  if (!id) return { error: 'ID inválido' }
  const supabase = await createClient()
  const { error } = await supabase.from('multas').delete().eq('id', id)
  if (error) return { error: 'Erro ao excluir multa' }
  revalidatePath('/sofia/multas')
  return { success: true }
}
```

Em `_table.tsx`, substituir `handleExcluirUma` (que usa `window.confirm`) por `DeleteConfirmButton`:

```tsx
import DeleteConfirmButton from '@/components/sofia/DeleteConfirmButton'

// Na coluna de ações, substituir o botão de excluir individual:
{isAdmin && (
  <DeleteConfirmButton action={excluirMultaAction} id={m.id} />
)}
```

- [ ] **Step 2: Sinistros — adicionar excluirSinistroAction + DeleteConfirmButton**

Em `app/(operacoes)/sofia/sinistros/_actions.ts`:

```ts
export async function excluirSinistroAction(
  _prev: { error?: string; success?: boolean },
  formData: FormData
): Promise<{ error?: string; success?: boolean }> {
  const id = formData.get('id') as string
  if (!id) return { error: 'ID inválido' }
  const supabase = await createClient()
  const { error } = await supabase.from('sinistros').delete().eq('id', id)
  if (error) return { error: 'Erro ao excluir sinistro' }
  revalidatePath('/sofia/sinistros')
  revalidatePath('/sofia/descontos')
  return { success: true }
}
```

Converter `sinistros/page.tsx` para client component (ou criar `_table.tsx` separado) para usar o DeleteConfirmButton. Adicionar coluna de delete:

```tsx
// Em sinistros/page.tsx, adicionar no thead:
<th className="px-4 py-3"></th>

// No tbody, última célula:
<td className="px-4 py-3 text-right">
  <DeleteConfirmButton action={excluirSinistroAction} id={s.id} />
</td>
```

> Sinistros/page.tsx é server component. Para usar DeleteConfirmButton (client), criar `_sinistros-table.tsx` como client component separado, igual ao padrão de multas (_table.tsx).

- [ ] **Step 3: Equipes — soft-delete**

Em `app/(operacoes)/sofia/equipes/_actions.ts`:

```ts
export async function desativarEquipeAction(
  _prev: { error?: string; success?: boolean },
  formData: FormData
): Promise<{ error?: string; success?: boolean }> {
  const id = formData.get('id') as string
  if (!id) return { error: 'ID inválido' }
  const supabase = await createClient()
  const { error } = await supabase.from('equipes').update({ ativo: false }).eq('id', id)
  if (error) return { error: 'Erro ao desativar equipe' }
  revalidatePath('/sofia/equipes')
  return { success: true }
}
```

Em `equipes/page.tsx`, nos cards de equipe, adicionar DeleteConfirmButton ao lado do botão ativar/desativar existente:

```tsx
// Criar componente client para o card ou converter page para client
// Nos cards:
<DeleteConfirmButton action={desativarEquipeAction} id={equipe.id} label="Desativar" />
```

- [ ] **Step 4: Revisões — excluirRevisaoAction + DeleteConfirmButton**

Em `app/(operacoes)/sofia/revisoes/_actions.ts`:

```ts
export async function excluirRevisaoAction(
  _prev: { error?: string; success?: boolean },
  formData: FormData
): Promise<{ error?: string; success?: boolean }> {
  const id = formData.get('id') as string
  if (!id) return { error: 'ID inválido' }
  const supabase = await createClient()
  const { error } = await supabase.from('revisoes').delete().eq('id', id)
  if (error) return { error: 'Erro ao excluir revisão' }
  revalidatePath('/sofia/revisoes')
  return { success: true }
}
```

Converter `revisoes/page.tsx` para client component ou criar `_table.tsx`. Adicionar coluna com DeleteConfirmButton.

- [ ] **Step 5: Checklist — excluirChecklistAction + DeleteConfirmButton**

Em `app/(operacoes)/sofia/checklist/_actions.ts`:

```ts
export async function excluirChecklistAction(
  _prev: { error?: string; success?: boolean },
  formData: FormData
): Promise<{ error?: string; success?: boolean }> {
  const id = formData.get('id') as string
  if (!id) return { error: 'ID inválido' }
  const supabase = await createClient()
  const { error } = await supabase.from('checklist').delete().eq('id', id)
  if (error) return { error: 'Erro ao excluir checklist' }
  revalidatePath('/sofia/checklist')
  return { success: true }
}
```

Em `checklist/page.tsx`, adicionar DeleteConfirmButton em cada linha (criar client component wrapper se necessário).

- [ ] **Step 6: Checar tipos**

```bash
npx tsc --noEmit
```

- [ ] **Step 7: Commit**

```bash
git add "app/(operacoes)/sofia/multas/" "app/(operacoes)/sofia/sinistros/" "app/(operacoes)/sofia/equipes/" "app/(operacoes)/sofia/revisoes/" "app/(operacoes)/sofia/checklist/"
git commit -m "feat: delete universal com DeleteConfirmButton em todos os módulos"
```

---

## Task 12: Cascade bidirecional — formulários de Multas e Sinistros

**Files:**
- Modify: `app/(operacoes)/sofia/multas/nova/_form.tsx`
- Modify: `app/(operacoes)/sofia/sinistros/novo/_form.tsx`

**Interfaces:**
- Consumes: `GET /api/sofia/veiculo-motorista` (Task 5)

- [ ] **Step 1: Cascade em NovaMultaForm**

Em `app/(operacoes)/sofia/multas/nova/_form.tsx`, adicionar state e handlers:

```tsx
'use client'
import { useActionState, useEffect, useState } from 'react'
// ... imports existentes

export default function NovaMultaForm({ veiculos, motoristas }: { veiculos: Veiculo[]; motoristas: Motorista[] }) {
  const [state, action, isPending] = useActionState(criarMultaAction, {})
  const [tipoInfracao, setTipoInfracao] = useState('')
  const [veiculoId, setVeiculoId] = useState('')
  const [motoristaId, setMotoristaId] = useState('')
  const router = useRouter()

  useEffect(() => { if (state.success) router.push('/sofia/multas') }, [state.success, router])

  async function handleVeiculoChange(id: string) {
    setVeiculoId(id)
    if (!id) { setMotoristaId(''); return }
    const res = await fetch(`/api/sofia/veiculo-motorista?veiculo_id=${id}`)
    const data = await res.json()
    if (data?.motoristas?.id) setMotoristaId(data.motoristas.id)
  }

  async function handleMotoristaChange(id: string) {
    setMotoristaId(id)
    if (!id) return
    const res = await fetch(`/api/sofia/veiculo-motorista?motorista_id=${id}`)
    const data = await res.json()
    if (data?.veiculo?.id) setVeiculoId(data.veiculo.id)
  }

  // Nos selects de veiculo e motorista, usar value + onChange:
  // <select name="veiculo_id" value={veiculoId} onChange={(e) => handleVeiculoChange(e.target.value)} ...>
  // <select name="motorista_id" value={motoristaId} onChange={(e) => handleMotoristaChange(e.target.value)} ...>
}
```

- [ ] **Step 2: Cascade em NovoSinistroForm**

Em `app/(operacoes)/sofia/sinistros/novo/_form.tsx`, adicionar os mesmos handlers, tomando cuidado para não quebrar o fluxo de fotos/upload já existente:

```tsx
// Adicionar ao estado existente:
const [veiculoId, setVeiculoId] = useState('')
const [motoristaId, setMotoristaId] = useState('')

async function handleVeiculoChange(id: string) {
  setVeiculoId(id)
  if (!id) { setMotoristaId(''); return }
  const res = await fetch(`/api/sofia/veiculo-motorista?veiculo_id=${id}`)
  const data = await res.json()
  if (data?.motoristas?.id) setMotoristaId(data.motoristas.id)
}

async function handleMotoristaChange(id: string) {
  setMotoristaId(id)
  if (!id) return
  const res = await fetch(`/api/sofia/veiculo-motorista?motorista_id=${id}`)
  const data = await res.json()
  if (data?.veiculo?.id) setVeiculoId(data.veiculo.id)
}

// Nos selects:
// veiculo_id: value={veiculoId} onChange={(e) => handleVeiculoChange(e.target.value)}
// motorista_id: value={motoristaId} onChange={(e) => handleMotoristaChange(e.target.value)}
```

- [ ] **Step 3: Checar tipos**

```bash
npx tsc --noEmit
```

- [ ] **Step 4: Commit**

```bash
git add "app/(operacoes)/sofia/multas/nova/_form.tsx" "app/(operacoes)/sofia/sinistros/novo/_form.tsx"
git commit -m "feat: cascade bidirecional veículo↔motorista em multas e sinistros"
```

---

## Task 13: Animações em todo o sistema

**Files:**
- Modify: todos os arquivos de página e componentes com elementos interativos

A estratégia é aplicar as classes de animação de forma sistemática usando grep para encontrar todos os elementos sem as classes corretas.

- [ ] **Step 1: Padrão de classes por tipo de elemento**

| Elemento | Classes a adicionar |
|---|---|
| `<button type="submit">` primário | adicionar `active:scale-95` |
| `<button>` secundário/link | adicionar `active:scale-95 transition-transform` |
| `<tr>` de tabela com `hover:bg-` | confirmar `transition-colors` |
| Card (`div rounded-xl`) que é link/clicável | adicionar `hover:border-[#f05a28] cursor-pointer transition-colors` |
| `<Link>` de navegação interna | adicionar `transition-colors` |
| Badges clicáveis | adicionar `hover:opacity-80 transition-opacity` |

- [ ] **Step 2: Aplicar em Sidebar**

Em `components/sofia/Sidebar.tsx`, nos links de navegação, adicionar `active:scale-95 transition-transform` nos itens do menu.

- [ ] **Step 3: Aplicar nos cards de Equipes**

Em `app/(operacoes)/sofia/equipes/page.tsx`, nos cards:

```tsx
<div key={equipe.id} className="rounded-xl border border-[#1e3a5f] bg-[#0d2050] p-5 hover:border-[#f05a28] transition-colors">
```

- [ ] **Step 4: Aplicar nos botões primários (todos os forms)**

Varrer todos os `<button type="submit">` com classe `bg-[#f05a28]` e adicionar `active:scale-95`:

```
Arquivos: todas as pages e _form.tsx sob app/(operacoes)/sofia/
Substituição: `bg-[#f05a28] text-white` → `bg-[#f05a28] text-white active:scale-95`
```

- [ ] **Step 5: Aplicar nas linhas de tabela clicáveis**

Confirmar que todos os `<tr>` com `hover:bg-[#0d2050]` também têm `transition-colors`. Verificar e adicionar onde faltar.

- [ ] **Step 6: Checar build**

```bash
npm run build
```

Esperado: build sem erros.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat: animações hover/active em todo o sistema (botões, cards, tabelas, sidebar)"
```

---

## Ordem de execução recomendada

```
Task 1 (SQL + types)
  ↓
Task 2 (helper autorizacao.ts)
  ↓
Task 3 (labels)    Task 4 (motoristas)    Task 5 (route handler)    Task 9 (locação)    Task 11 (deletes)    Task 13 (animações)
                                              ↓
                                   Task 6 (revisões)    Task 7 (KM gatilho)    Task 12 (cascade forms)
                                                              ↓
                                                         Task 8 (descontos)
                                                              ↓
                                                         Task 10 (custos)
```

Tasks 3, 4, 9, 11, 13 podem rodar em paralelo após Task 1+2.
Tasks 6, 7, 12 podem rodar em paralelo após Task 5.
Task 8 depende de Task 7. Task 10 é independente mas usa dados de KM.
