# D2.1 — correção simplificada, mensagem do Duda

**Recebido em 14/09/2026**, colado no chat pelo João. Resposta à mensagem pós-conselho
(`docs/cliente/2026-08-31-sistema-controle-de-obras/conselho-revisao-d21-2026-09-14.md`).
Texto literal, sem edição.

> João, apliquei a correção simplificada da D2.1 e publiquei na branch
> `duda/field-api-client`.
>
> Commit: `3edba98`
>
> Ficou exatamente nesta regra:
>
> 1. OS com `archived === true` na listagem é filtrada: não cria obra, não conta como
>    presente, não limpa alerta e não atualiza obra existente.
> 2. A herança só acontece quando o GET direto de `/orders/:id` devolve `archived === true`.
>    404, 422, campo ausente ou falha não herdam.
> 3. Se o `field_id` antigo veio na mesma varredura, não há consulta nem herança; o conflito
>    fica visível no relatório.
> 4. O motivo devolvido pela consulta chega ao relatório como veio, sem categorias
>    adicionais.
>
> Também removi os comportamentos que você mandou desconsiderar: não existe mais herança
> baseada em `archived:true` da própria listagem nem separação entre falha passageira e
> decisão manual.
>
> Validação em Node 20:
> - 337/337 testes de Obras, 14 suítes;
> - TypeScript limpo;
> - lint sem erros, apenas os 3 avisos antigos do Sofia;
> - build concluído.
>
> Não fiz chamadas reais ao Field, não escrevi no banco e não comecei a D3.
