# D2.1 — mensagem de entrega do Duda

**Recebido em 14/09/2026**, colado no chat pelo João. Texto literal, sem edição (a
mensagem veio com um resumo seguido do texto completo; os dois estão aqui).

> D2.1 concluída e publicada na branch `duda/field-api-client`.
>
> - Commit: `6f5aa96`
> - Base: `origin/master` `671949b`
> - Branch limpa e sincronizada
> - Nenhuma migration nova necessária
> - Nenhuma chamada real ao Field ou escrita em produção
> - D3 não iniciada
>
> Entraram a herança conservadora, N1, N3 e também os ajustes baratos N2, N4 e detecção de
> página repetida do N5. A requisição extra quando o total é múltiplo de 100 foi mantida,
> pois removê-la exigiria voltar a confiar no `totalCount` potencialmente desatualizado.
>
> Validação: 332/332 testes de Obras, 14 suítes, TypeScript sem erros, lint sem erros e
> build concluído.
>
> João, concluí a D2.1 e publiquei na branch `duda/field-api-client`, commit `6f5aa96`,
> baseada no master `671949b`.
>
> Implementei a herança conservadora da OS reaberta:
>
> - identifica previamente o conflito de número com `field_id` novo;
> - consulta a ordem antiga por `GET /orders/:field_id`;
> - antiga arquivada ou 404: troca o `field_id`, preserva todo o histórico, limpa
>   suspeita/alerta e relata "OS reaberta: histórico herdado";
> - antiga ativa: não herda e mantém conflito visível;
> - resposta sem `archived` booleano ou falha: fica inconclusiva, não herda e tenta
>   novamente numa próxima execução;
> - troca de números entre duas obras continua como conflito.
>
> A consulta ficou isolada em função própria e há testes para ativa, arquivada, inexistente,
> resposta inconclusiva e falha.
>
> Também entraram:
>
> - N1: alertas já confirmados não contam no numerador do disjuntor;
> - N2: piso absoluto de 3 ausências antes do bloqueio em bases pequenas;
> - N3: tolerância de 20 horas para a confirmação diária;
> - N4: Field e base vazios não geram aviso inútil;
> - N5: página repetida é detectada cedo e interrompe a leitura. Mantive a página extra
>   quando o total é múltiplo de 100, pois evitá-la exigiria confiar novamente no
>   `totalCount`.
>
> Não foi necessária migration nova. Não usei chave real, não consultei o Field real e não
> toquei em produção.
>
> Validação em Node 20: 332/332 testes de Obras, 14 suítes, TypeScript sem erro, lint sem
> erro e build concluído.
>
> A D3 não foi iniciada; aguardo o merge da D2.1.
