# Investigação — 185 OS (nossa extração) x 25 atividades (contagem do João no Field)

**15/09/2026.** Só leitura na API do Field (GET), com a mesma chave de `.env.local`.
Nenhum código do app foi alterado; nada foi escrito em produção. Os dados usados vêm de
duas fontes: o cache local desta investigação (`ordens-cru-cache.json` e
`tasks-cru-cache.json`, no scratchpad da sessão — não commitados) e um punhado de
chamadas novas, só leitura, para responder o que o cache não tinha (lista de `/services`,
`/work-services`, `/tasks` global, e o campo `archived` de cada atividade).

**Veredito curto: a hipótese de "tipo de atividade diferente de tipo de OS" está
DESCARTADA pelos dados — a API não tem esse campo.** A explicação mais provável agora é
algo na TELA do Field (paginação, busca por texto em vez de filtro estruturado, ou
escopo da conta/equipe) — nenhuma dessas dá para confirmar por API; só o painel do João
confirma. Ver §5.

---

## 1. Que serviço é o nosso `service_id`?

**Não é um valor fixo (env/constante/banco) — é resolvido em tempo de execução, pelo
NOME, a cada varredura** (com cache em memória do processo, não persistido):

- A busca é pelo nome `"Atividade Spot"`, constante `NOME_PADRAO_DO_TIPO_DE_OS`
  (`app/obras/_lib/field/cliente.ts:22`).
- A resolução roda em `resolverIdDoTipoDeOs()` (`cliente.ts:158-173`): chama
  `GET /services?q=name:"Atividade Spot"` e aplica `escolherTipoDeOs()`
  (`cliente.ts:102-127`) para desempatar se vier mais de um resultado.
- A sincronização real não passa `nomeDoTipoDeOs` nem `estrategiaDeLoja` —
  `app/obras/sincronizar/_execucao.ts:190` chama `criarClienteField({ chaveApi })` só
  com a chave, então usa esse padrão.

**O que a API devolve para `/services` (todos os 10 tipos de OS cadastrados, sem
filtro):**

| id | name | archived | createdAt |
|---|---|---|---|
| `NTA0MjIxOjk1NzMx` | Instalação | **true** | 2026-06-12 |
| `NTA0MjIyOjk1NzMx` | Manutenção | **true** | 2026-06-12 |
| `NTA1MjAzOjk1NzMx` | Manutenção Preventiva | false | 2026-06-16 |
| `NTA1MjA0Ojk1NzMx` | Manutenção Corretiva | false | 2026-06-16 |
| `NTA1MjA1Ojk1NzMx` | Emergencial | false | 2026-06-16 |
| **`NTA1MjA2Ojk1NzMx`** | **Atividade Spot** | false | 2026-06-16 |
| `NTA1MjA3Ojk1NzMx` | Levantamento/Orçamento | false | 2026-06-16 |
| `NTA1MjE0Ojk1NzMx` | Limpeza de Fachada | false | 2026-06-16 |
| `NTA1NjcwOjk1NzMx` | Dedetização/Limpeza de Caixa D'Água | false | 2026-06-19 |
| `NTA1NjgxOjk1NzMx` | Revisão de Gerador | false | 2026-06-19 |

**PROVADO: só existe UM tipo chamado "Atividade Spot"**, nome exato, sem ambiguidade —
`escolherTipoDeOs` bate no desempate mais simples (nome idêntico único), sem precisar de
critério de desempate. Não há tipo parecido nem duplicado. **Esta hipótese (id errado
ou tipo ambíguo) está descartada.**

## 2. Como a API chama "atividade" — existe tipo/categoria própria?

**Não existe.** Testado de três formas:

1. **O objeto da atividade** (`GET /orders/:id/tasks`, item do array `items`) tem estes
   campos: `id, duration, position, status, startedAt, completedAt, statusDescription,
   ratingLink, description, viewedAt, receivedAt, archived, createdAt, updatedAt,
   metadata, statusClassification, employee, order, scheduling, coords, startCoords,
   completeCoords`. **Nenhum campo de tipo/categoria/serviço.** O único vínculo com
   "Atividade Spot" é indireto: `order: { id }` aponta para a OS, e é a OS que tem
   `service`.
2. **`GET /work-services`** (o recurso de faturamento com nome parecido, mas
   documentado como *outro* recurso — ver `api-field-control-levantamento.md` §2)
   respondeu 200 com **`totalCount: 0`** — a conta não tem nenhum item cadastrado ali.
   Não é isso.
3. **`GET /tasks`** (endpoint global, não documentado, mas existe e responde 200) —
   testado com quatro filtros diferentes no `q`
   (`service_id:"..."`, `order_service_id:"..."`, `type:"Atividade Spot"`, e um campo
   inventado só para forçar erro): **os quatro devolveram exatamente o mesmo
   `totalCount: 5927`.** Ou seja, **esse endpoint global ignora silenciosamente
   qualquer filtro em `q`** — não é 422, não filtra, só devolve tudo. `5927` é o total
   de atividades da conta inteira, de todos os tipos de OS, e não serve para isolar
   "Spot" por essa via.

**Conclusão provada: uma "atividade" não tem tipo próprio na API — o único jeito de uma
atividade ser "do tipo Spot" é a OS-mãe dela ser do tipo Spot.** Não existe, na API, um
segundo critério de classificação de atividade que pudesse divergir do critério de OS.

## 3. Quantas OS têm pelo menos uma atividade Spot

Consequência direta do item 2: como toda atividade "é Spot" só por herdar da OS, e todas
as 185 OS trazidas pela varredura são do tipo Spot, **as 185 têm, por definição, pelo
menos uma atividade Spot** (nenhuma tem zero atividades — conferido na exportação:
`zeroAtividades: 0`). O total de atividades sob essas 185 OS é **210** (mín. 1, máx. 4
por OS; 164 OS com exatamente 1, 18 com 2, 2 com 3, 1 com 4).

**Nem 185 nem 210 se aproxima de 25 por este caminho.** A hipótese "o filtro dele é por
tipo de atividade, o nosso é por tipo de OS, e são coisas diferentes" não se sustenta,
porque na API eles são a mesma coisa — não existe uma segunda classificação para
divergir.

## 4. O que a listagem NÃO filtra (confirmado com dado)

| Filtro que a listagem NÃO aplica | O que os dados mostram |
|---|---|
| `archived` da OS | **0 de 185** OS vieram com `archived: true` (inclusive a 7777, já reportado antes) |
| `archived` da atividade | Das 210 atividades, **205 `false` / 5 `true`**. Só **2 de 185 OS têm TODAS as atividades arquivadas**, e só **4 de 185 têm a ÚLTIMA atividade arquivada**. Não é um recorte que produza 25 — **hipótese "atividade arquivada explica o sumiço" também descartada** |
| Status da última atividade | Nenhum filtro — a listagem traz todos os status. Contagem real (via `triagem-suspeitas-2026-09-15.md`, gerada sem nova chamada): `Resolvido` 58, `done` 51, `pending` 45, `scheduled` 13, `Orçamento Aguardando Aprovação` 4, `Falta de Tempo` 3, `reported` 2, `Fechar OS` 2, e sete valores únicos (1 cada) — nenhuma combinação simples desses valores fecha em 25 |
| `updated_at` | Não entra na carga completa (`cliente.ts:179-182` + `_execucao.ts:140`) |

## 5. Hipóteses ordenadas pela evidência

| # | Hipótese | Evidência a favor | Status | Teste exato no painel |
|---|---|---|---|---|
| ~~A~~ | `service_id` errado / tipo ambíguo | — | **DESCARTADA** — só 1 tipo chamado "Atividade Spot", resolução exata sem ambiguidade (§1) | — |
| ~~B~~ | Atividade tem tipo próprio, diferente do tipo da OS | — | **DESCARTADA** — API não tem esse campo (§2) | — |
| ~~C~~ | Atividade arquivada some do filtro do João | 5/210 arquivadas | **DESCARTADA** — nem perto de explicar 185→25 (§4) | — |
| **D** | **A tela do Field pagina em 25 por página, e "25" é o tamanho da página, não o total** | Muito comum em telas de lista (25/50/100 é o default típico) | **Mais provável, não testado** | Abrir a tela "Atividades" com o mesmo filtro e olhar se existe indicador "mostrando 1–25 de N" ou botão de próxima página/"carregar mais" embaixo da lista |
| **E** | **O "filtro" foi busca de TEXTO livre pela frase "atividade spot", não um filtro estruturado por tipo** | Se for campo de busca (não *dropdown* de "Tipo de OS"/"Tipo de Atividade"), ele casa só com atividades cujo texto contém literalmente essas palavras — um recorte bem mais estreito que "todas as OS do tipo Atividade Spot" | **Segunda mais provável, não testado** | Repetir usando o filtro **estruturado** de tipo (um *dropdown*/seletor de "Tipo de OS", não a lupa de busca), se a tela tiver os dois separados; comparar o resultado |
| **F** | **A tela está com escopo diferente do que a chave da API enxerga** (conta/cliente, equipe/técnico, ou período) | A nossa base tem **2 clientes concentrando as 185 OS** (134 e 49, mais 2 contas com 1 cada) e **4 técnicos/pessoas diferentes como `createdBy`**; se a tela do João estiver presa a um cliente ou período específico, o total dela é menor por natureza | Possível, mas nenhum recorte batido nos nossos dados fecha em 25 sozinho | Conferir se a tela tem filtro de cliente/conta, responsável/equipe ou período ativo além do "situação: todas", e se sim, desligar todos antes de contar de novo |
| **G** | João contou errado / tela com bug momentâneo | — | Sem evidência a favor nem contra | Recontar do zero, com F já verificado |

**O que ficou provado, sem depender do painel:** o `service_id` que a sincronização usa
é o correto e único "Atividade Spot" (§1); não existe, na API, nenhum campo de
tipo/categoria de atividade que pudesse divergir do tipo da OS (§2); nem `archived` de
OS, nem `archived` de atividade, nem status da última atividade explicam sozinhos uma
queda de 185 para 25 (§4). **O que continua hipótese, e só o painel resolve:** se "25" é
o total real da tela ou só o tamanho de uma página (D), se o filtro usado foi busca de
texto em vez de tipo estruturado (E), e se a tela tem escopo de conta/equipe/período
diferente do da chave de API (F).

**Recomendação:** não puxar nada para produção até D e E serem checados no painel — são
os mais baratos de testar e, se qualquer um deles for a causa, o `service_id` e a
sincronização continuam corretos (nada a mudar no código); só o número "25" que o João
viu estava sendo mal lido, não os 185 da nossa extração.

---

## Rodada 2 — o 24 do João

**Correção do João: não é 25, é 24**, contado na tela **Operação > Atividade**, filtro
"atividade spot", **todas as situações**. Unidade de contagem: **atividades**, não OS —
diferente da rodada 1, que comparava 185 (OS) com um número solto. Isso muda o
denominador certo: comparar 24 contra as **210 atividades** das 185 OS "Atividade
Spot" (não contra 185).

**Sem chamar a API de novo** — reaproveitado o cache local desta sessão (185 OS crua +
tasks de cada uma, já buscadas na Rodada 1 para checar `archived`).

### O que foi testado

**1) Por status (todas as 210 atividades, não só a última de cada OS):**

| Status bruto (`status`) | Atividades |
|---|---|
| done | 128 |
| pending | 53 |
| scheduled | 14 |
| reported | 13 |
| in-progress | 1 |
| on-route | 1 |

Nenhum valor isolado nem as combinações plausíveis de "não concluída" batem em 24:

| Combinação | Atividades | OS distintas |
|---|---|---|
| pending + scheduled | 67 | 67 |
| pending + scheduled + in-progress | 68 | 68 |
| pending + scheduled + in-progress + on-route | 69 | 68 |
| pending + scheduled + in-progress + on-route + reported (= tudo, exceto `done`) | 82 | 76 |
| scheduled + in-progress + on-route | 16 | 16 |
| reported sozinho | 13 | 12 |

**2) Por período**, em 5 campos de data candidatos (`scheduling.date`, `startedAt`,
`completedAt`, `updatedAt`, `createdAt`) × 6 janelas (hoje, esta semana, este mês,
últimos 7/15/30 dias) — **nenhuma janela isolada, em nenhum campo, bate em 24.** A mais
perto: `scheduling.date` "últimos 15 dias" = 47; `updatedAt` "hoje" = 82. Não há meio-
termo natural que caia em 24 sozinho.

**3) Por responsável e por cliente:** 19 técnicos/pessoas diferentes em `employee.id`
(mais 97 atividades sem responsável atribuído); 4 clientes na base, concentrados em 146
e 62 atividades. Nenhum isolado bate em 24.

**4) Não arquivada cruzada com status + período** (a combinação mais "de tela de
operação real": atividade aberta, não arquivada, dentro de uma janela) — testada
inteira, none bate em 24/23/25. A mais próxima, e a única que é uma combinação **natural**
(dimensões que uma tela de operação realmente cruzaria):

> **status não concluída + não arquivada + tem `scheduling.date` preenchido = 28
> atividades, 26 OS.** Perto de 24, mas não é 24.

### Busca exaustiva (2 dimensões)

Como nenhum recorte de 1 dimensão nem a combinação operacional natural bateram, rodei
uma varredura cruzando **todas as 48 dimensões testadas, duas a duas (1.128 pares)** —
status, arquivamento, responsável (com/sem) e as 30 combinações de campo-de-data×janela.
**36 desses 1.128 pares batem exatamente em 23, 24 ou 25.**

**Isso é o esperado ao testar mais de mil combinações contra uma faixa de 3 valores —
a maioria desses 36 achados é coincidência estatística, não um filtro real.** A prova:
praticamente todos cruzam duas dimensões que uma tela nunca cruzaria ao mesmo tempo sem
o usuário configurar os dois manualmente (ex.: `startedAt` "últimos 15 dias" **e**
`createdAt` "este mês" — dois campos de data diferentes, ao mesmo tempo, não é like um
filtro de tela única).

**Um achado, porém, é limpo e merece ser levado ao João para conferência linha a
linha**, porque bate exatamente em 24 com uma leitura de negócio plausível
("atividades agendadas para este mês que ainda não têm técnico atribuído no Field") e é
1 atividade = 1 OS (sem dupla contagem):

**`sem responsável` + `scheduling.date: este mês` = exatamente 24 atividades, 24 OS
distintas:**

| Nº da OS | Loja | Status | Data agendada |
|---|---|---|---|
| 861 | AV NOSSA SENHORA DE COPACABANA, 766, COPACABANA, RIO DE JANEIRO, RJ | Resolvido | 15/09 |
| 885 | AV. RODOLFO DE AMOEDO, S/N, BARRA DA TIJUCA, RIO DE JANEIRO, RJ | Resolvido | 15/09 |
| 900 | ROD AMARAL PEIXOTO, 2289, CENTRO, IGUABA GRANDE, RJ | Resolvido | 15/09 |
| 956 | R JULIO DE CASTILHOS, 23, COPACABANA, RIO DE JANEIRO, RJ | Resolvido | 15/09 |
| 997 | EST DOS BANDEIRANTES, 27, TAQUARA, RIO DE JANEIRO, RJ | Resolvido | 15/09 |
| **7777** | Avenida Ayrton Senna, 2150, Barra da Tijuca, RJ | Resolvido | 14/09 |
| DP BARRA DA TIJUCA 5 - CAMINHO DO CLIENTE | AV EMBAIXADOR ABELARDO BUENO, 2840, BARRA DA TIJUCA, RJ | done | 04/09 |
| DP BOTAFOGO 10 | R GENERAL POLIDORO, 108, BOTAFOGO, RJ | done | 15/09 |
| DP CATETE 2 - PORTA PRINCIPAL | R DO CATETE, 248, CATETE, RJ | done | 14/09 |
| DP CATETE 5 - RETIRADA DE ADESIVO | R DO CATETE, 300, CATETE, RJ | done | 15/09 |
| DP COPACABANA 6 - CAMINHÃO DE FUCSÃO | AV N S DE COPACABANA, 534, COPACABANA, RJ | done | 14/09 |
| DP COPACABANA 6 - REPARO DE PORTA NA RETAGUARDA | AV N S DE COPACABANA, 534, COPACABANA, RJ | done | 14/09 |
| DP ITABORAI - PINTURA BANHEIRO FEMININO | AV 22 DE MAIO, 5550, ITABORAI, RJ | done | 15/09 |
| DP ITABORAI - TELAMILIMETRICA | AV 22 DE MAIO, 5550, ITABORAI, RJ | done | 15/09 |
| DP LEBLON 4 - CASA DE BOMBA | AV BARTOLOMEU MITRE, 310, LEBLON, RJ | done | 15/09 |
| DP LEME - BANDEJAMENTO FORRO | R GUSTAVO SAMPAIO, 650, LEME, RJ | done | 14/09 |
| DP MAGÉ 1 - FINALIZAÇÃO DE OBRA | R DR. SIQUEIRA, 349, CENTRO, MAGE, RJ | done | 14/09 |
| DP MARICA 1 - DESRATIZAÇÃO | R RIBEIRO DE ALMEIDA, 56, CENTRO, MARICA, RJ | done | 14/09 |
| DP PETROPOLIS 2 - IMPERMEABILIZAÇÃO DE MARQUISE E PINTURA DA FACHADA | R DO IMPERADOR, 271, CENTRO, PETROPOLIS, RJ | done | 09/09 |
| DP PIABETA - FINALIZAÇÃO DE OBRA | AV SANTOS DUMONT, 213, PIABETA, MAGE, RJ | done | 14/09 |
| DP RECREIO 7 | Avenida Ayrton Senna, 2150, Barra da Tijuca, RJ | Resolvido | 14/09 |
| MUTIRÃO TMO119 | Rua Do Imperador, 9 e 11, Centro, Petrópolis, RJ | Resolvido | 11/09 |
| PLK-NORTESHOP | Avenida Dom Hélder Câmara, 5332, Cachambi, RJ | in-progress | 15/09 |
| VISA108 | Rua Doutor Nilo Peçanha, 6, Centro, São Gonçalo, RJ | Resolvido | 11/09 |

Note que a maioria já está com status `done`/`Resolvido` — ou seja, **este recorte não é
"pendente de atribuição" no sentido óbvio** (técnico já foi lá e resolveu, só não ficou
registrado quem na API). Isso enfraquece a leitura de negócio que tornaria este achado
plausível — é provavelmente coincidência também, só que com uma contagem 1:1 conveniente
para conferência manual.

### Resposta objetiva

**Nenhum filtro único e natural — status isolado, período isolado, responsável, cliente,
ou a combinação operacional óbvia (aberta + não arquivada + com data) — reproduz 24 com
confiança.** O achado exato mais citável (`sem responsável` + `scheduling.date: este
mês` = 24) exige um segundo critério ("sem técnico atribuído") que o João não mencionou
usar, e o próprio conteúdo do recorte (maioria já `done`) contradiz a leitura de negócio
que o justificaria — **tratar como candidato para conferência manual, não como
explicação confirmada.**

**O que isso significa para a primeira carga:** a pergunta "185 (universo do serviço) ou
só o subconjunto que a tela mostra" continua em aberto, porque não achamos QUAL
subconjunto é esse. Como nenhum critério de dado (status, arquivamento, data, técnico,
cliente) isola 24 de forma limpa e defensável, a explicação mais provável continua do
lado da TELA (Rodada 1, hipóteses D/E/F: paginação, busca de texto em vez de filtro
estruturado, ou escopo de conta/equipe) — **não um recorte de dado que a nossa
sincronização devesse aplicar.** Recomendo o João, no painel: (1) abrir a lista com o
filtro "atividade spot" e ativamente checar se existe paginação/"mostrando X de Y"
abaixo da lista; (2) comparar a lista de 24 OS acima, linha a linha, com o que aparece
na tela dele — se baterem, o critério real é "sem responsável + agendada este mês", e aí
sim teríamos achado; se não baterem, a causa é outra e ainda não identificada. Até lá,
**não recomendo restringir a carga a um subconjunto sem critério confirmado** — o
universo correto e defensável continua sendo as 185 OS do tipo "Atividade Spot", que é
exatamente o que o cliente pediu para conferir no Excel (`relatorio-exportacao-2026-09-15.md`,
`triagem-suspeitas-2026-09-15.md`).
